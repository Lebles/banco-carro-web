export const Label= ({ children }) => 
                    <label style={{ display: "inline-block" }}>{children}</label>;
export const Input= ({ type }) => <input type={type} style={{ marginLeft: 10 }}/>;