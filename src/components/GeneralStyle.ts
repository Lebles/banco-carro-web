import { CSSProperties } from "react";

const style = {
    centrar: {
        minHeight: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    } as CSSProperties,
    input_text: {
        fontSize: 16,
        padding: 5,
        borderRadius: 5,
        margin: 10,
        width: 300
    } as CSSProperties,
};

export default style;